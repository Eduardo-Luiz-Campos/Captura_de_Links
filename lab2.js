// lab02.js — Ponto de partida
// (reutilize buscarHTML e extrairLinks do Exercício 1)

const DOMINIO_BASE = 'books.toscrape.com';

function classificarLinks(links, dominio) {

    // TODO: separar links em internos e externos
    // Dica: use link.startsWith('/') || link.includes(dominio)
    return { internos: [], externos: [] };
}

// TODO: chamar buscarHTML, extrairLinks e depois classificarLinks
// e imprimir os dois grupos com os totais